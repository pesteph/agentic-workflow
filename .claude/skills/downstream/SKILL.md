---
name: downstream
description: Install or upgrade the agentic-workflow methodology into your user scope (~/.claude, ~/.copilot). Run from a clone of this repo.
---

# Downstream (bootstrap entry)

This thin registration exists only so `/downstream` is runnable as a slash command from a fresh clone of this repo. It is **not** the source — to avoid drift, the canonical, full instructions live in one place.

**Read [`skills/downstream/SKILL.md`](../../../skills/downstream/SKILL.md) (and its `adapters.md`) in this repo, and follow it exactly.**
